apiVersion: v1
kind: Pod
metadata:
  annotations:
    avp_path: {{ .Values.avppath }}
  labels:
    run: ubuntu
  name: {{ .Values.name }}
spec:
  containers:
  - image: ubuntu
    env:
    - name: DEMO_GREETING
      value: {{ .Values.testvalue2 }}
    name: {{ .Values.name }}
    command: ["sleep", "inf"]
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
